STAGE 15 continued - real conversation memory + widget redesign
===================================================================

EXTRACTION - same as always: extract straight into your project folder,
choose overwrite, confirm app/src/main/res/drawable/ic_vince_triangle.xml
exists afterward (new file - if it's missing, extraction didn't take).

WHAT'S FIXED THIS ROUND
--------------------------

1. REAL CONVERSATION MEMORY (the CLARA screenshot bug)
   Found the actual cause: every message sent to the AI was completely
   stateless - only your newest typed message was ever sent, with zero
   awareness of anything said earlier in that same thread. That's
   exactly why CLARA forgot what "that answer" meant one message later,
   and then didn't even recognize its own previous reply (7.65).
   Fixed: every message now carries the recent conversation (up to the
   last 20 messages in that thread) along with it, so the AI can
   actually reason from what was already said - "multiply that by 130"
   will now correctly resolve to the previous answer, across VINCE,
   CLARA, and DAVINA alike. This is the flowing-conversation behavior
   you asked for.

2. FLOATING WIDGET - REDESIGNED
   - Circular, medium-sized bubble (was a big square before).
   - At rest, it now shows the actual VINCE triangle logo (first real
     version of the locked icon design) instead of a plain colored
     circle with a letter.
   - Tap to start a voice command: a colored halo ring appears around
     the bubble in the active persona's color while it's listening,
     then disappears once you're done talking.
   - Hold to switch persona still works the same as before - only the
     halo's color changes to match, the logo itself stays constant
     (it's the app's own mark, not persona-specific).
   - Drag still works the same as before.

3. REMINDERS - PARKED per your call
   No further reminder patches this round since you've said you won't
   be using that feature. Nothing removed, it's just not getting more
   debugging effort until/unless you want to revisit it.

TESTING CHECKLIST
--------------------
1. Confirm ic_vince_triangle.xml exists under app/src/main/res/drawable/.
2. Open a chat, ask CLARA (or any persona) a multi-step question like
   the "45% of 17, now multiply that by 130" example - confirm it
   actually carries the thread across replies now.
3. Turn on the floating widget - confirm it's circular, medium-sized,
   and shows the triangle logo at rest.
4. Tap it, confirm the halo ring appears in the current persona's color
   while listening, and disappears after.
5. Hold it, confirm persona switches and the halo color changes to
   match on the next tap.
